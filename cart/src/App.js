import React from 'react'
import logo from './logo.svg'
import './App.css'

const App = ({name}) => (
    <div className="App-content topped padded">
      <h1>{name}</h1>
    </div>
  )

export default App
